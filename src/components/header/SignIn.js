import React from 'react';
import style from '../../css_moduls/header.module.css'

const SignIn = () => {
    return (
        <button className={`${style.btnSign}`}>
            Sign in
        </button>
    );
};

export default SignIn;